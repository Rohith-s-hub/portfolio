import { Link } from "react-router-dom";
import { ArrowUp } from "lucide-react";
import { author } from "../data/content";

const columns=[{title:"Explore",links:[["Blog","/blog"],["Projects","/projects"],["About","/about"],["Now","/now"],["Uses","/uses"]]},{title:"Elsewhere",links:[["GitHub",author.github],["LinkedIn",author.linkedin],["Email",`mailto:${author.email}`],["Resume","/resume"]]},{title:"Signal",links:[["RSS feed","/rss.xml"],["Sitemap","/sitemap.xml"],["Contact","/contact"]]}];
export default function Footer(){return <footer className="site-footer"><div className="section-shell"><div className="footer-grid"><div><div className="footer-brand">Rohith R.</div><p className="footer-copy">Building grounded AI products, secure systems, and the occasional tool that should have existed already.</p></div>{columns.map(col=><div className="footer-col" key={col.title}><h3>{col.title}</h3>{col.links.map(([label,href])=>href.startsWith("http")||href.startsWith("mailto")?<a href={href} target={href.startsWith("http")?"_blank":undefined} rel="noreferrer" key={label}>{label}</a>:<Link to={href} key={label}>{label}</Link>)}</div>)}</div><div className="footer-bottom"><span>© {new Date().getFullYear()} Rohith R. / Built deliberately.</span><button onClick={()=>window.scrollTo({top:0,behavior:"smooth"})} className="arrow-link" aria-label="Back to top"><ArrowUp size={16}/></button></div></div></footer>}
