import { Link } from "react-router-dom";
import { ArrowRight } from "lucide-react";
import { author, certifications, education, experience, skills } from "../data/content";

export default function About(){return <><section className="page-shell"><div className="section-shell about-feature"><div className="about-feature__copy"><div className="eyebrow">About / Rohith R.</div><h1 className="page-title">Build.<br/>Then<br/>polish.</h1><p>{author.bio}</p><p className="muted">Based in {author.location}. I’m focused on roles where strong engineering judgment and product curiosity are expected—not optional.</p><div className="hero-actions"><a className="cta" href={author.github} target="_blank" rel="noreferrer">GitHub</a><a className="secondary-cta" href={author.linkedin} target="_blank" rel="noreferrer">LinkedIn</a></div></div><div className="about-feature__image"><img src={author.avatar} alt="Rohith R"/></div></div></section><section className="editorial-section editorial-section--dark"><div className="section-shell"><div className="section-kicker">Approach</div><h2 className="section-title">Learn in public. Build under constraint.</h2><div className="timeline">{[["01","Start close to the problem","A project earns its stack by solving a real friction."],["02","Make the hard parts visible","Architecture, trade-offs, and failure modes are part of the work."],["03","Ship before certainty","A useful release gives you information a perfect plan cannot."],["04","Document the lessons","The build log keeps the next version more honest."]].map(([num,title,copy])=><article className="timeline-item" key={num}><small>{num}</small><h3>{title}</h3><p>{copy}</p></article>)}</div></div></section><section className="editorial-section"><div className="section-shell"><div className="section-head"><div><div className="section-kicker">Experience & education</div><h2 className="section-title">The timeline so far.</h2></div></div><div className="uses-grid">{experience.map(e=><article className="list-card" key={e.company}><h3>{e.company}</h3><p className="muted">{e.role} · {e.period}</p><ul>{e.highlights.map(h=><li key={h}>{h}</li>)}</ul></article>)}{education.slice(0,2).map(e=><article className="list-card" key={e.institution}><h3>{e.degree}</h3><p className="muted">{e.institution} · {e.period}</p><ul><li>{e.score}</li><li>{e.location}</li></ul></article>)}</div></div></section><section className="editorial-section editorial-section--cream"><div className="section-shell"><div className="section-head"><div><div className="section-kicker">Toolbox</div><h2 className="section-title">Tools used with intent.</h2></div></div><div className="uses-grid">{Object.entries(skills).slice(0,6).map(([group,items])=><article className="list-card" key={group}><h3>{group}</h3><ul>{items.slice(0,6).map(item=><li key={item}>{item}</li>)}</ul></article>)}</div><div className="now-banner" style={{marginTop:"3rem"}}><div><div className="section-kicker">Next conversation</div><h2 className="section-title">Have a useful problem?</h2></div><Link to="/contact" className="secondary-cta">Contact me <ArrowRight size={16}/></Link></div></div></section></>}
