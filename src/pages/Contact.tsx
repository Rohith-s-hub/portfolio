import { useState } from "react";
import { ArrowUpRight } from "lucide-react";
import { author } from "../data/content";
export default function Contact(){const [form,setForm]=useState({name:"",email:"",message:""});const submit=(e:React.FormEvent)=>{e.preventDefault();const subject=encodeURIComponent(`Portfolio contact from ${form.name||"a visitor"}`);const body=encodeURIComponent(`From: ${form.name}\nEmail: ${form.email}\n\n${form.message}`);window.location.href=`mailto:${author.email}?subject=${subject}&body=${body}`};return <div className="page-shell"><section className="section-shell"><header className="page-intro"><div className="eyebrow">Open channel</div><h1 className="page-title">Make it<br/>specific.</h1><p className="page-intro__body">For projects, engineering roles, or a sharp question about one of the builds: email is the fastest path. I read every message.</p></header><div className="contact-grid"><a className="contact-link" href={`mailto:${author.email}`}><span>Direct email</span><strong>{author.email}</strong><ArrowUpRight/></a><a className="contact-link" href={author.github} target="_blank" rel="noreferrer"><span>Source work</span><strong>GitHub /<br/>Rohith-s-hub</strong><ArrowUpRight/></a><a className="contact-link" href={author.linkedin} target="_blank" rel="noreferrer"><span>Professional profile</span><strong>LinkedIn /<br/>Rohith</strong><ArrowUpRight/></a><a className="contact-link" href="/resume"><span>Role context</span><strong>Read the<br/>résumé</strong><ArrowUpRight/></a></div><div className="form-shell"><div className="section-kicker">Email composer</div><h2 className="section-title">Start the message.</h2><p className="page-intro__body">This opens your email client; nothing is silently collected here.</p><form onSubmit={submit}><div className="form-grid"><div><label htmlFor="name">Name</label><input id="name" required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div><label htmlFor="email">Email</label><input id="email" type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div></div><div><label htmlFor="message">What are you working on?</label><textarea id="message" rows={7} required value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></div><button className="cta" type="submit">Open email draft <ArrowUpRight size={16}/></button></form></div></section></div>}
