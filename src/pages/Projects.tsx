import { useMemo, useState } from "react";
import ProjectCard from "../components/ProjectCard";
import { projects } from "../data/content";

export default function Projects(){const [filter,setFilter]=useState("All");const categories=useMemo(()=>["All",...new Set(projects.map(p=>p.category))],[]);const shown=filter==="All"?projects:projects.filter(p=>p.category===filter);return <div className="page-shell"><section className="section-shell"><header className="page-intro"><div className="eyebrow">Portfolio / selected work</div><h1 className="page-title">Systems<br/>over<br/>demos.</h1><p className="page-intro__body">AI tools, security platforms, and full-stack applications built around real operational problems—not an invented brief.</p></header><div className="stat-strip">{[[`${projects.length}`,"Documented builds"],["AI", "Core capability"],["OSS", "Open source work"],["2024—26", "Active years"]].map(([value,label])=><div className="stat-card" key={label}><div className="stat-card__value">{value}</div><div className="stat-card__label">{label}</div></div>)}</div><div className="filter-bar" style={{marginTop:"2rem"}}>{categories.map(c=><button key={c} className={`filter-button ${filter===c?"active":""}`} onClick={()=>setFilter(c)}>{c}</button>)}</div><div className="projects-grid">{shown.map((project,index)=><ProjectCard key={project.id} project={project} index={index}/>)}</div></section></div>}
